## ADDED Requirements

### Requirement: English UI Translation
The system SHALL display all hardcoded user interface text in English instead of Vietnamese.

#### Scenario: User opens the application
- **WHEN** user views any screen or modal within the frontend UI
- **THEN** all static text (buttons, labels, headers) is displayed in English
